
def Ucs:
    this.treeNode.add(new Node("starting",this,null,this.state,0,0))
    while treeNode !=None:
        this.visits=this.visits+1
        print(visits)

        Node minCostNode = None
        mincost=0 #this will need to become as big as possible Max_Value
        for i in range(treeNode.size()):
            if this.treeNode
